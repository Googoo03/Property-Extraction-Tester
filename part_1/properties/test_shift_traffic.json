[
  {
    "scope": "function",
    "function": "shift_traffic",
    "property": "preserves_length",
    "formal": "len(output) == len(input)"
  },
  {
    "scope": "branch",
    "function": "shift_traffic",
    "condition": "len(current) != len(target)",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError('shape mismatch')"
  },
  {
    "scope": "branch",
    "function": "shift_traffic",
    "condition": "not current",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError('empty allocation')"
  },
  {
    "scope": "loop",
    "function": "shift_traffic",
    "property": "loop_invariant",
    "formal": "all(updated[idx] == current[idx] + (target[idx] - current[idx]) * damping for idx in range(len(current)))"
  },
  {
    "scope": "return",
    "function": "shift_traffic",
    "property": "return_postcondition",
    "formal": "all(abs(updated[idx] - (current[idx] + (target[idx] - current[idx]) * damping)) < 1e-9 for idx in range(len(current)))"
  }
]