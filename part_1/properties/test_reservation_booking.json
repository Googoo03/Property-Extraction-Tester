[
  {
    "scope": "function",
    "function": "reservation_booking",
    "property": "preserves_length",
    "formal": "len(output) == len(input) + 1 if start < end and not any_overlap else len(input)"
  },
  {
    "scope": "branch",
    "function": "reservation_booking",
    "condition": "start >= end",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError if start >= end"
  },
  {
    "scope": "loop",
    "function": "reservation_booking",
    "property": "loop_invariant",
    "formal": "no_overlap_holds = all(end <= s or start >= e for s, e in existing[:i])"
  },
  {
    "scope": "branch",
    "function": "reservation_booking",
    "condition": "not (end <= s or start >= e)",
    "property": "branch_specific_behavior",
    "formal": "returns (False, existing) if overlap detected"
  },
  {
    "scope": "return",
    "function": "reservation_booking",
    "property": "return_postcondition",
    "formal": "returns (False, existing) if overlap exists"
  },
  {
    "scope": "return",
    "function": "reservation_booking",
    "property": "return_postcondition",
    "formal": "returns (True, updated) if no overlap, where updated is sorted(existing + [interval])"
  }
]