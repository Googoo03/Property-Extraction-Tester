[
  {
    "scope": "function",
    "function": "plan_delivery_slot",
    "property": "preserves_length",
    "formal": "len(output) == len(input) + 1 if output[0] else len(input)"
  },
  {
    "scope": "branch",
    "function": "plan_delivery_slot",
    "condition": "a >= b",
    "property": "branch_specific_behavior",
    "formal": "output == (False, timeline)"
  },
  {
    "scope": "branch",
    "function": "plan_delivery_slot",
    "condition": "any(not (b <= s or a >= e) for s, e in timeline)",
    "property": "branch_specific_behavior",
    "formal": "output == (False, timeline)"
  },
  {
    "scope": "return",
    "function": "plan_delivery_slot",
    "property": "return_postcondition",
    "formal": "output[0] == False and output[1] == timeline"
  },
  {
    "scope": "return",
    "function": "plan_delivery_slot",
    "property": "return_postcondition",
    "formal": "output[0] == True and output[1] == sorted(timeline + [window])"
  }
]