[
  {
    "scope": "function",
    "function": "tip_share",
    "property": "preserves_length",
    "formal": "len(output) == len(input)"
  },
  {
    "scope": "branch",
    "function": "tip_share",
    "condition": "len(ratios) == 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"no ratios\")"
  },
  {
    "scope": "branch",
    "function": "tip_share",
    "condition": "sum(ratios) <= 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"invalid ratios\")"
  },
  {
    "scope": "branch",
    "function": "tip_share",
    "condition": "amount < 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"negative amount\")"
  },
  {
    "scope": "return",
    "function": "tip_share",
    "property": "return_postcondition",
    "formal": "all(s >= -fee for s in output) and sum(output) == amount - fee"
  },
  {
    "scope": "return",
    "function": "tip_share",
    "property": "return_postcondition",
    "formal": "all(abs(output[i] - ((ratios[i] / sum(ratios)) * amount - fee)) < 1e-9 for i in range(len(ratios)))"
  },
  {
    "scope": "function",
    "function": "tip_share",
    "property": "fee_application",
    "formal": "fee is subtracted once from the total amount distributed"
  }
]