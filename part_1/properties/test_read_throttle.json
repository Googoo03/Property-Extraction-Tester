[
  {
    "scope": "function",
    "function": "read_throttle",
    "property": "preserves_length",
    "formal": "len(timestamps) == len([t for t in timestamps if t >= now - window]) or len(timestamps) > len([t for t in timestamps if t >= now - window])"
  },
  {
    "scope": "loop",
    "function": "read_throttle",
    "property": "loop_invariant",
    "formal": "all(t >= now - window for t in [t for t in timestamps if t >= now - window])"
  },
  {
    "scope": "branch",
    "function": "read_throttle",
    "condition": "len(active) > limit",
    "property": "branch_specific_behavior",
    "formal": "output == (False, 0) if len(active) > limit else output == (True, limit - len(active))"
  },
  {
    "scope": "return",
    "function": "read_throttle",
    "property": "return_postcondition",
    "formal": "output == (False, 0) if len([t for t in timestamps if t >= now - window]) > limit else output == (True, limit - len([t for t in timestamps if t >= now - window]))"
  },
  {
    "scope": "return",
    "function": "read_throttle",
    "property": "return_postcondition",
    "formal": "output[0] == (len([t for t in timestamps if t >= now - window]) <= limit)"
  }
]