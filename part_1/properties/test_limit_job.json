[
  {
    "scope": "function",
    "function": "limit_job",
    "property": "preserves_length",
    "formal": "len(output) == len(input)"
  },
  {
    "scope": "loop",
    "function": "limit_job",
    "property": "loop_invariant",
    "formal": "iteration preserves semantic rule"
  },
  {
    "scope": "branch",
    "function": "limit_job",
    "condition": "len(recent) > limit",
    "property": "branch_specific_behavior",
    "formal": "output behavior depends on condition"
  },
  {
    "scope": "return",
    "function": "limit_job",
    "property": "return_postcondition",
    "formal": "returns value satisfying expected semantics: return False, 0"
  },
  {
    "scope": "return",
    "function": "limit_job",
    "property": "return_postcondition",
    "formal": "returns value satisfying expected semantics: return True, limit - len(recent)"
  },
  {
    "scope": "precondition",
    "function": "limit_job",
    "property": "input_validation",
    "formal": "len(timestamps) >= 0 and now >= 0 and window > 0 and limit >= 0"
  },
  {
    "scope": "condition",
    "function": "limit_job",
    "property": "cutoff_calculation",
    "formal": "cutoff = now - window"
  },
  {
    "scope": "condition",
    "function": "limit_job",
    "property": "recent_calculation",
    "formal": "recent = [t for t in timestamps if t >= cutoff]"
  },
  {
    "scope": "condition",
    "function": "limit_job",
    "property": "limit_check",
    "formal": "len(recent) <= limit"
  }
]