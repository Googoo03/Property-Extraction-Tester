[
  {
    "scope": "function",
    "function": "shift_demand",
    "property": "preserves_length",
    "formal": "len(output) == len(input)"
  },
  {
    "scope": "branch",
    "function": "shift_demand",
    "condition": "len(current) != len(target)",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError('shape mismatch')"
  },
  {
    "scope": "branch",
    "function": "shift_demand",
    "condition": "not current",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError('empty allocation')"
  },
  {
    "scope": "loop",
    "function": "shift_demand",
    "property": "loop_invariant",
    "formal": "for all i, adjusted[i] = current[i] + (target[i] - current[i]) * damping"
  },
  {
    "scope": "return",
    "function": "shift_demand",
    "property": "return_postcondition",
    "formal": "returns list where each element is adjusted as per damping formula"
  }
]