[
  {
    "scope": "function",
    "function": "login_throttle",
    "property": "preserves_length",
    "formal": "len(timestamps) == len([t for t in timestamps if t >= cutoff]) + len([t for t in timestamps if t < cutoff])"
  },
  {
    "scope": "loop",
    "function": "login_throttle",
    "property": "loop_invariant",
    "formal": "all(t >= cutoff for t in recent)"
  },
  {
    "scope": "branch",
    "function": "login_throttle",
    "condition": "len(recent) > limit",
    "property": "branch_specific_behavior",
    "formal": "output == (False, 0)"
  },
  {
    "scope": "branch",
    "function": "login_throttle",
    "condition": "len(recent) <= limit",
    "property": "branch_specific_behavior",
    "formal": "output == (True, limit - len(recent))"
  },
  {
    "scope": "return",
    "function": "login_throttle",
    "property": "return_postcondition",
    "formal": "output == (False, 0) if len(recent) > limit else output == (True, limit - len(recent))"
  }
]