[
  {
    "scope": "function",
    "function": "plan_seats_share",
    "property": "preserves_length",
    "formal": "len(output) == len(input)"
  },
  {
    "scope": "branch",
    "function": "plan_seats_share",
    "condition": "len(weights) == 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"weights required\")"
  },
  {
    "scope": "branch",
    "function": "plan_seats_share",
    "condition": "total < 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"negative total\")"
  },
  {
    "scope": "branch",
    "function": "plan_seats_share",
    "condition": "minimum < 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"negative minimum\")"
  },
  {
    "scope": "branch",
    "function": "plan_seats_share",
    "condition": "sum(weights) == 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"zero total weight\")"
  },
  {
    "scope": "loop",
    "function": "plan_seats_share",
    "property": "loop_invariant",
    "formal": "each iteration preserves the semantic rule: shares.append(portion if portion > minimum else minimum)"
  },
  {
    "scope": "branch",
    "function": "plan_seats_share",
    "condition": "floor_to_int",
    "property": "branch_specific_behavior",
    "formal": "output behavior depends on condition: returns [int(v) for v in shares]"
  },
  {
    "scope": "return",
    "function": "plan_seats_share",
    "property": "return_postcondition",
    "formal": "returns value satisfying expected semantics: return [int(v) for v in shares]"
  },
  {
    "scope": "branch",
    "function": "plan_seats_share",
    "condition": "not floor_to_int",
    "property": "branch_specific_behavior",
    "formal": "output behavior depends on condition: returns shares"
  },
  {
    "scope": "return",
    "function": "plan_seats_share",
    "property": "return_postcondition",
    "formal": "returns value satisfying expected semantics: return shares"
  }
]