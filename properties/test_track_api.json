[
  {
    "scope": "function",
    "function": "track_api",
    "property": "preserves_length",
    "formal": "len(output) == len(input)"
  },
  {
    "scope": "branch",
    "function": "track_api",
    "condition": "max_value is not None",
    "property": "branch_specific_behavior",
    "formal": "output behavior depends on condition"
  },
  {
    "scope": "branch",
    "function": "track_api",
    "condition": "new_value > max_value",
    "property": "branch_specific_behavior",
    "formal": "output behavior depends on condition"
  },
  {
    "scope": "return",
    "function": "track_api",
    "property": "return_postcondition",
    "formal": "returns value satisfying expected semantics: return new_value"
  },
  {
    "scope": "precondition",
    "function": "track_api",
    "property": "key_exists",
    "formal": "key in counts or counts.get(key, 0) == 0"
  },
  {
    "scope": "precondition",
    "function": "track_api",
    "property": "max_value_non_negative",
    "formal": "max_value is None or max_value >= 0"
  },
  {
    "scope": "postcondition",
    "function": "track_api",
    "property": "value_incremented",
    "formal": "counts[key] == (input_counts.get(key, 0) + 1) or counts[key] == max_value"
  },
  {
    "scope": "postcondition",
    "function": "track_api",
    "property": "no_new_keys_created",
    "formal": "len(counts) == len(input_counts) or (len(counts) == len(input_counts) + 1 and key not in input_counts)"
  },
  {
    "scope": "branch",
    "function": "track_api",
    "condition": "max_value is None",
    "property": "unbounded_increment",
    "formal": "counts[key] == input_counts.get(key, 0) + 1"
  },
  {
    "scope": "branch",
    "function": "track_api",
    "condition": "new_value <= max_value",
    "property": "within_bounds",
    "formal": "counts[key] == input_counts.get(key, 0) + 1"
  }
]