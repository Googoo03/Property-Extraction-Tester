[
  {
    "scope": "function",
    "function": "storage_quota_split",
    "property": "preserves_length",
    "formal": "len(output) == len(input)"
  },
  {
    "scope": "branch",
    "function": "storage_quota_split",
    "condition": "total < 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"negative total\")"
  },
  {
    "scope": "branch",
    "function": "storage_quota_split",
    "condition": "not ratios or sum(ratios) <= 0",
    "property": "branch_specific_behavior",
    "formal": "raises ValueError(\"invalid ratios\")"
  },
  {
    "scope": "loop",
    "function": "storage_quota_split",
    "property": "loop_invariant",
    "formal": "all(s >= 0 for s in shares) and sum(shares) == total"
  },
  {
    "scope": "return",
    "function": "storage_quota_split",
    "property": "return_postcondition",
    "formal": "all(s - fee >= 0 for s in shares) and sum(output) == total - len(shares) * fee"
  },
  {
    "scope": "loop",
    "function": "storage_quota_split",
    "property": "loop_invariant",
    "formal": "all(s >= 0 for s in shares) and sum(shares) == total"
  }
]